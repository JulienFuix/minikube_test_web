apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: web-monitor
  labels:
    app: basicwebapp
spec:
  selector:
    matchLabels:
      app: basicwebapp
  endpoints:
  - port: "9090"
    path: /metrics
  namespaceSelector:
    matchNames:
    - default
